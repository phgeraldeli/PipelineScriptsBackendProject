{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "1.2.209",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "node app.js",
    "test:mocha": "nyc --reporter=lcov mocha --exit --timeout 10000",
    "test": "istanbul cover _mocha -- ./test/*test.js --include-all-sources --exit true"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.0",
    "nyc": "^11.8.0",
    "request": "^2.83.0",
    "supertest": "^3.0.0"
  }
}
